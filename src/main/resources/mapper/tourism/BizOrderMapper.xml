<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qinhu.oasis.tourism.mapper.BizOrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_order (
            order_no, order_type, user_id, parking_space_id, vehicle_no,
            start_time, end_time, total_amount, paid_amount, status, remark
        ) VALUES (
            #{orderNo}, #{orderType}, #{userId}, #{parkingSpaceId}, #{vehicleNo},
            #{startTime}, #{endTime}, #{totalAmount}, #{paidAmount}, #{status}, #{remark}
        )
    </insert>

    <select id="selectById" resultType="com.qinhu.oasis.tourism.entity.BizOrder">
        SELECT * FROM biz_order WHERE id = #{id} AND deleted = 0
    </select>

    <update id="updateStatus">
        UPDATE biz_order SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
