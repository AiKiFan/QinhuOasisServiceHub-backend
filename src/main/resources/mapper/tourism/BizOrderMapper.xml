<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qinhu.oasis.tourism.mapper.BizOrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_order (
            order_no, order_type, user_id, parking_space_id, vehicle_no,
            start_time, end_time, total_amount, paid_amount, status, remark
        ) VALUES (
            #{orderNo}, #{orderType}, #{userId}, #{parkingSpaceId}, #{vehicleNo},
            #{startTime}, #{endTime}, #{totalAmount}, #{paidAmount}, #{status}, #{remark}
        )
    </insert>

    <select id="selectById" resultType="com.qinhu.oasis.tourism.entity.BizOrder">
        SELECT * FROM biz_order WHERE id = #{id} AND deleted = 0
    </select>

    <update id="updateStatus">
        UPDATE biz_order SET status = #{status} WHERE id = #{id}
    </update>

    <!-- 查询超时未入场的停车订单（入场时间超过30分钟仍未入场） -->
    <select id="selectTimeoutParkingOrders" resultType="com.qinhu.oasis.tourism.entity.BizOrder">
        SELECT * FROM biz_order
        WHERE deleted = 0
          AND order_type = 2          <!-- 停车订单 -->
          AND status = 0             <!-- 待入场 -->
          AND TIMESTAMPDIFF(MINUTE, start_time, NOW()) > 30
    </select>

    <!-- 更新订单取消信息 -->
    <update id="updateCancelInfo">
        UPDATE biz_order
        SET status = #{status},
            cancel_reason = #{cancelReason},
            cancelled_by = #{cancelledBy}
        WHERE id = #{id}
    </update>

    <!-- 更新订单实付金额（停车结算时使用） -->
    <update id="updatePaidAmount">
        UPDATE biz_order
        SET paid_amount = #{paidAmount},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
