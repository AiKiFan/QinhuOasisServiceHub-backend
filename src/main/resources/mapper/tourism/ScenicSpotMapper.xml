<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qinhu.oasis.tourism.mapper.ScenicSpotMapper">

    <select id="selectAll" resultType="com.qinhu.oasis.tourism.entity.ScenicSpot">
        SELECT * FROM biz_scenic_spot
        WHERE deleted = 0 AND status = 1
        ORDER BY sort_score DESC
    </select>

    <select id="selectById" resultType="com.qinhu.oasis.tourism.entity.ScenicSpot">
        SELECT * FROM biz_scenic_spot
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByIds" resultType="com.qinhu.oasis.tourism.entity.ScenicSpot">
        SELECT * FROM biz_scenic_spot
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
        ORDER BY sort_score DESC
    </select>

    <select id="selectPage" resultType="com.qinhu.oasis.tourism.entity.ScenicSpot">
        SELECT * FROM biz_scenic_spot
        WHERE deleted = 0 AND status = 1
        ORDER BY sort_score DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM biz_scenic_spot
        WHERE deleted = 0 AND status = 1
    </select>

    <select id="selectTopByScore" resultType="com.qinhu.oasis.tourism.entity.ScenicSpot">
        SELECT * FROM biz_scenic_spot
        WHERE deleted = 0 AND status = 1
        ORDER BY sort_score DESC
        LIMIT #{limit}
    </select>

</mapper>