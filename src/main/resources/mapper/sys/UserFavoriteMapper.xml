<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qinhu.oasis.sys.mapper.UserFavoriteMapper">

    <insert id="insert">
        INSERT INTO biz_user_favorite (user_id, target_type, target_id, folder_id)
        VALUES (#{userId}, #{targetType}, #{targetId}, #{folderId})
    </insert>

    <!-- 恢复软删除的收藏记录（重新收藏时复用 -->
    <update id="restore">
        UPDATE biz_user_favorite
        SET deleted = 0, folder_id = #{folderId}, create_time = NOW()
        WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId} AND deleted = 1
    </update>

    <update id="deleteByUserAndTarget">
        UPDATE biz_user_favorite
        SET deleted = 1
        WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId} AND deleted = 0
    </update>

    <select id="selectByUserAndTarget" resultType="com.qinhu.oasis.sys.entity.UserFavorite">
        SELECT * FROM biz_user_favorite
        WHERE user_id = #{userId} AND target_type = #{targetType} AND target_id = #{targetId} AND deleted = 0
    </select>

    <select id="selectByUserAndType" resultType="com.qinhu.oasis.sys.entity.UserFavorite">
        SELECT * FROM biz_user_favorite
        WHERE user_id = #{userId} AND target_type = #{targetType} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByUser" resultType="com.qinhu.oasis.sys.entity.UserFavorite">
        SELECT * FROM biz_user_favorite
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectTargetIdsByUserAndType" resultType="long">
        SELECT target_id FROM biz_user_favorite
        WHERE user_id = #{userId} AND target_type = #{targetType} AND deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>